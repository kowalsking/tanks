export const WIDTH = 1024
export const HEIGHT = 768
export const ENEMY_COUNT = 10

export const resizer = {
  center: {
    x: WIDTH / 2,
    y: HEIGHT / 2
  }
}

export enum Turn {
  UP = 0,
  RIGHT = 90,
  DOWN = 180,
  LEFT = 270
}
